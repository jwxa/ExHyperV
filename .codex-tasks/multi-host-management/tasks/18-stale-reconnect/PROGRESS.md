# Issue #18 进度

## 当前状态

- 状态：DONE
- 当前切片：5，兼容性与完成门禁。
- TDD 阶段：GREEN，完成检查通过。
- 基线提交：`0aec6c4`。
- 基线验证：204/204；Release 构建 0 错误、246 个既有警告。

## 已确认边界

- 复用 `HostSessionRegistry` 为每个远程宿主持有的独立 `ActiveHostSessionCoordinator`，不复制重连引擎。
- 多宿主注册表是 #18 的行为验证面；旧协调器测试只作为算法基础证据。
- 旧数据保留在现有 `HostVmGroupViewModel.Vms`，连接状态变化不能触发清空。
- 使用现有成功、警告、错误主题色；旧数据原因通过 Tooltip 提示。
- 不执行真实断网或任何远程写操作。

## 恢复点

1. 添加 A 掉线但 B 继续工作的注册表 RED 测试。
2. 添加 A/B 并行重连、目标宿主停止与立即重试测试。
3. 添加 VM 分组旧数据保留及状态 Tooltip 测试。
4. 只实现 RED 暴露出的最小生产改动。

## 失败记录

- 第一次 RED 在测试编译期失败：测试项目未链接完整 `ExHyperV.Models`。已按现有选择性链接模式改为只链接真实 `HostVmGroupViewModel`，并以最小 VM 行桩验证集合保留，避免把整套 VM 详情依赖引入会话测试。
- 有效 RED：207/208；多宿主重连隔离、并行重连和定向立即重试均已通过，唯一失败为 VM 分组缺少 `StatusText` 旧数据说明。
- GREEN：208/208；分组在不增加可见布局的前提下，通过标题 Tooltip 区分正在重连、等待重试和已停止的旧数据状态。

## 完成结果

1. A 掉线只把 A 标记为重连中和旧数据；A 的写入、读取刷新和控制台被能力矩阵阻止，B 的 stamp、读取、写租约与控制台继续有效。
2. A/B 可以同时持有各自唯一的重连任务；重复掉线报告不会启动第二个任务，B 可在 A 仍旧数据时独立恢复。
3. “立即重试”和“停止重连”显式携带选中宿主的 `HostId`；停止后保留旧数据和分组，不回退本机。
4. 重连成功只推进目标宿主代次、重算能力并触发目标 VM 分组刷新。
5. VM 分组继续复用现有警告色，在标题 Tooltip 中说明正在重连、等待重试或已停止以及当前为旧数据。
6. 最终验证：209/209；Release 构建 0 错误、246 个既有警告；UTF-8 无 BOM、凭据模式和差异检查通过。

## 下一步

- #19：实现按宿主写计数和控制台窗口状态驱动的安全主动断开。
