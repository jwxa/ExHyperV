# Issue #17 进度

## 当前状态

- 状态：DONE
- 当前切片：5，兼容性与完成门禁。
- TDD 阶段：GREEN。
- 基线提交：`a369e7b`。
- 基线验证：200/200；Release 构建 0 错误。

## 已确认边界

- 复用现有 `IHostSessionRegistry`、`IHostOperationRouter`、`HostVmGroupViewModel` 和统一详情模板。
- 远程宿主按注册表快照顺序呈现，不按名称或地址重新排序。
- 每台远程宿主独立监控，不能用一个串行循环遍历所有远程组。
- 页面选择不能成为后端路由；批量命令继续以单一 `HostId` 为硬门槛。

## 完成结果

1. VM 页面投影注册表中的全部宿主，本机固定第一，远程按连接顺序显示。
2. 手动刷新并行执行，每台远程宿主持有独立监控与操作取消范围。
3. A/B 的写租约、会话代次和旧结果相互隔离，旧 stamp 不进入错误宿主后端。
4. 跨宿主选择会清空其他列表，选择更新显式携带唯一 `HostId`。
5. RED 为 200/204，GREEN 为 204/204；Release 构建 0 错误、246 个既有警告；UTF-8 无 BOM、凭据模式和差异检查通过。

## 下一步

- #18：补充旧数据展示与每宿主独立自动重连交互。
