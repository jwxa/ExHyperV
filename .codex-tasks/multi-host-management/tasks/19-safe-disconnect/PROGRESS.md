# Issue #19 进度

## 当前状态

- 状态：DONE
- 当前切片：5，兼容性与完成门禁。
- TDD 阶段：GREEN，完成检查通过。
- 基线提交：`5b351b2`。
- 基线验证：209/209；Release 构建 0 错误、246 个既有警告。

## 已确认边界

- 断开必须显式携带目标 `HostId`，本机不可断开。
- 写租约检查与断开冻结必须原子化，避免确认期间新写入产生竞态。
- 控制台注册表只维护窗口生命周期，不承担会话状态职责。
- 保存配置由 `HostProfileManager` 独立维护，主动断开不得删除配置或凭据。
- 不执行真实远程断开或其他危险验收动作。

## 恢复点

1. 添加目标宿主写租约阻止断开、其他宿主不受影响的 RED 测试。
2. 添加断开准备后拒绝新写入、取消后恢复写入、提交后移除会话的 RED 测试。
3. 添加按宿主控制台登记与只关闭目标窗口的 RED 测试。
4. 添加断开确认、取消、页面按钮和 VM 分组移除测试。
5. 只实现 RED 暴露出的最小生产改动。

## 失败记录

- 有效 RED：测试项目编译失败，缺少 `HostDisconnectAvailability`、`IHostDisconnectPreparation`、`HostDisconnectResult`、注册表断开 API 和快照写计数。
- 第二个有效 RED：测试项目编译失败，缺少 `IHostConsoleWindow` 和按宿主维护的控制台窗口注册表。
- 第三个有效 RED：测试项目编译失败，缺少 `HostDisconnectCoordinator`、确认提示和工作流结果契约。
- 第四个有效 RED：按钮接线和 VM 分组移除的源码契约失败；现有页面只有连接路径，注册表移除事件被直接忽略。
- 第一次 UI GREEN 回归为 219/220：旧测试仍要求按钮 Tooltip 固定绑定 `ConnectHint`。新断开门控需要动态 Tooltip，已将该过时断言更新为 `ConnectionActionToolTip`。

## 下一步

- #20：实现按选中宿主过滤的实时脱敏日志。

## 完成结果

1. 注册表快照发布每宿主写计数；断开可用性、准备、取消和提交全部按 `HostId` 原子协调，新写入与新控制台在准备后被拒绝。
2. 控制台窗口通过窄注册表按宿主和 VM 登记、激活、注销与批量关闭；断开 A 不关闭 B 的窗口。
3. 无控制台时直接断开；有控制台时弹窗显示宿主名、窗口数量和影响范围；取消零副作用，确认期间出现新写入时在关窗前安全中止。
4. 主按钮只显示“连接到此主机”“正在连接”或“断开”；目标宿主有写操作时按钮置灰，Tooltip 显示具体数量和原因。
5. 主动断开停止目标重连并释放会话，VM 页面取消目标组任务并移除目标远程组；本机、其他远程组和保存配置保持。
6. 最终验证：221/221；Release 构建 0 错误、246 个既有警告；UTF-8 无 BOM、凭据模式和差异检查通过。
