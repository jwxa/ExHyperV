# Issue #22 进度

## 当前状态

- 状态：DONE（本地实现和全部验收完成；GitHub Issue 保持 OPEN，尚未 push）。
- 基线提交：`fce49fa`。
- 最新验证：226/226；Release/WPF 构建 0 错误、247 个既有警告。

## 已确认边界

- 注册表是运行时唯一多宿主事实来源；页面选择只改变 UI 上下文。
- 每宿主内部连接引擎保留，不用无收益改名扩大风险。
- 本机专属页面固定路由到 `HostId.Local`。
- 远程配置、回滚、VM 写入和故障注入继续需要各自精确中文 `确认`。
- 用户此前停止桌面自动化，当前不会擅自恢复。

## 已完成

- 删除全局活动宿主接口、静态会话、VM 操作和控制台兼容适配器；产品入口统一读取 `IHostSessionRegistry`。
- 保留 `ActiveHostSessionCoordinator` 作为每宿主内部连接与重连引擎，避免无收益的大范围改名。
- VM、控制台和本机专属操作全部显式携带 `HostId`；本机固定存在，断开只移除目标远程宿主。
- 受控验收运行器覆盖本机与远程并行、真实 VM 读取、TCP 2179 控制台捕获和主动断开。
- 主机连接、VM 状态和日志界面统一使用 WPF-UI 主题资源。完成度复审发现旧截图中的暗色日志正文为黑色、亮色禁用连接按钮对比度不足；当前工作树已修复并加入合同测试。
- README、隐私政策和远程宿主管理指南已更新为多宿主模型。

## 验证证据

- 单元与契约测试：226/226 通过。
- Release/WPF/集成构建：0 错误；247 个既有警告。
- `10.0.0.6` 最终只读验收：7 通过、6 安全跳过、0 失败；WMI/DCOM、TCP 2179、1 台 VM、控制台捕获和主动断开均通过。
- 危险开关：VM 写入、故障注入、远程配置、回滚复检全部为 `false`。
- 静态门禁：32 个 XAML 可解析；46 个变更文本 UTF-8 无 BOM；敏感字面量、禁用硬编码颜色和 `git diff --check` 通过。
- 报告：`controlled-host-readonly-final-20260815.json`。
- 旧视觉证据：`raw/ui/` 下 8 张截图及同名文本断言。它们覆盖布局，但同时暴露了本次已修复的主题对比度问题，不能再作为修复后的最终视觉通过证据。
- 修复后视觉证据：2026-08-16 用户手工确认亮色/暗色、宽/窄四种状态均通过；日志来源/消息和禁用连接按钮可读，页面无错位、重叠或文字截断。

## 剩余风险

- 第二台真实远程宿主、TCP 2179 阻断、VM 写入、远程配置、回滚和断线注入未执行；前两项由确定性测试覆盖，后四项仍需各自精确中文 `确认`。
- 用户此前停止桌面自动化；修复后视觉结论来自用户手工复核，未生成或伪报自动化截图。
- Release 构建保留 247 个仓库既有警告，以及 `SSH.NET 2024.1.0` 的已知安全公告；均未在本 Issue 扩大范围。

## 下一步

- 提交人工视觉结论和最终验收记录；不 push，不关闭 Issue。
- 按维护者安排进行后续远端评审与合并。
