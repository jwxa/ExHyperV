# Issue #16 进度

## 当前状态

- 状态：DONE
- 当前切片：全部完成。
- TDD 阶段：GREEN。
- 基线提交：`e3fde3d`。
- 最新验证：200/200；Release 构建 0 错误。

## 已完成

- 切片 1：`IHostOperationRouter.ReadAsync` 只根据传入 `HostId` 捕获本机或远程 WMI context，并复用脱敏、旧结果和连接丢失分类。
- 切片 2：本机与远程写租约按 `HostId` 隔离；目标会话变旧时写结果返回 `Stale`，其他宿主 stamp 不受影响。
- 切片 3：应用级共享注册表接入主机连接页；最新诊断成功后添加远程会话，移除“切回本机”入口和活动宿主切换措辞。
- 切片 4：虚拟机页始终保留本机组，并在首台远程连接后追加远程组；VM 行使用宿主范围内的 `VmKey`。
- 切片 5：加载、刷新、电源与控制台均按 VM 所属 `HostId` 路由；本机与远程复用同一列表和详情视觉，远程永久不支持项隐藏。
- 切片 6：200/200 确定性测试通过；Release 构建 0 错误、246 个既有警告；UTF-8 无 BOM、XAML 编译和差异检查通过。
- 受控宿主只读验收：`raw/controlled-host-issue16-readonly-20260815.json` 对 `10.0.0.6` 使用 Credential Manager 凭据完成 7 项、跳过 6 项、失败 0 项；WMI/DCOM、TCP 2179、1 台 VM 读取和控制台捕获通过，四个危险开关全为 `false`。

## 已确认边界

- VM 读写只通过 `IHostOperationRouter` 接收 `HostId`。
- VM 分组只通过注册表不可变快照建立，不读取页面“活动宿主”。
- 连接页必须使用当前诊断报告创建连接请求，不能恢复旧的单活动宿主切换。

## 恢复点

1. 独立提交 #16。
2. 从 #17 继续多台远程宿主并行管理。
