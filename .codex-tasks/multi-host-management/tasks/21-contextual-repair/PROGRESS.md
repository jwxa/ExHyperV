# Issue #21 进度

## 当前状态

- 状态：DONE
- 当前切片：5，兼容性与完成门禁已完成。
- TDD 阶段：GREEN，最终自动化审计通过。
- 基线提交：`a881d38`。
- 基线验证：230/230；Release 构建 0 错误。

## 已确认边界

- 现有预检、规划、命令编译、精确确认、回滚和复诊流水线继续作为唯一实现。
- 新增纯决策负责“是否显示入口”和不可自动处理的中文引导，不把该逻辑散落到 XAML。
- 修复上下文绑定配置 ID、地址和诊断开始时间，不依赖页面显示名。
- 预检仍先只读；用户打开入口不会触发远程配置命令。
- 不执行真实远程配置、回滚、VM 写入或故障注入。

## 恢复点

1. 为入口判定、中文引导和诊断版本上下文添加 RED 测试。
2. 最小实现纯诊断决策与上下文。
3. 替换常驻 UI 入口并绑定当前选择/最新诊断。
4. 流式发布预检证据并在应用前拒绝旧上下文。
5. 运行安全流水线与完成门禁。

## 失败记录

- 第一个有效 RED：测试项目编译失败，缺少诊断修复决策和绑定配置 ID、地址、诊断时间的上下文类型。
- 第二个有效 RED：页面源码契约失败，仍渲染常驻“配置预检”，没有按诊断结果控制的动作、中文引导和隐藏修复工作区。
- 第三个有效 RED：测试项目编译失败，预检流水线缺少逐条日志观察入口，预检 ViewModel 也无法在保留证据的同时失效旧批准计划。

## 下一步

- 进入 #22，清理旧活动宿主兼容路径并完成最终发布验收；真实 WPF 的亮/暗、宽/窄视觉门禁在 #22 统一执行。

## 已完成

- 已审阅 GitHub #21、正式规格、现有页面入口、预检 ViewModel、配置流水线和回滚实现，确认本次采用替换式接入而非重写配置脚本。
- 切片 1：新增纯 `HostRepairAdvisor` 和 `HostRepairContext`；仅权限不足、管理可用但 TCP 2179 可检查故障提供入口，密码/网络/命名空间等不可安全自动处理问题只返回中文引导；235/235 测试通过。
- 切片 2：移除常驻“配置预检”，当前最新诊断按需显示“检查并修复设置”；打开时捕获修复上下文，修复 Tab 只在工作流打开后可见，选择或新诊断关闭旧工作区，不可处理问题显示中文引导；236/236 测试与 Release XAML 构建通过。
- 切片 3：预检流水线为每条只读日志同步通知 UI，阻塞读取期间已有带时间证据到达；配置完成后失效旧批准但保留预览/结果，确认弹窗返回后再次校验宿主与诊断版本；238/238 测试通过。
- 切片 4：逐条映射 GitHub #21 的 9 条验收标准；现有配置测试覆盖 Public 网络显式选择、非精确确认零修改、最小权限/CIDR、精确回滚和执行后复诊/能力刷新；修复真实密码错误位于身份步骤而被漏判的中文引导问题；239/239 测试通过。
- 切片 5：239/239 确定性测试通过；因已运行的管理员实例锁定默认输出目录，改用独立临时输出完成同一 Release/WPF 构建，结果为 0 错误和 246 个既有警告；13 个变更文本文件均为 UTF-8 无 BOM，新增敏感字面量和硬编码颜色均为 0，`git diff --check` 通过。用户此前停止桌面自动化，因此视觉实机门禁保留到 #22，未虚报为已验证。
