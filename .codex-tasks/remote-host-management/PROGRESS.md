# Progress Log

## Context Recovery Block

- **Current child**: #12 - GitHub issue #13 end-to-end verification and release docs
- **Current status**: IN_PROGRESS
- **Last completed**: Issue #13 local acceptance tests, documentation, and release verification
- **Current artifact**: `.codex-tasks/remote-host-management/SUBTASKS.csv`
- **Key context**: Issues #2 through #11 are complete. Issues #12 and #13 have complete local implementations and automated verification. The suite passes 178/178; the Release build has 0 errors; XAML, UTF-8, CSV, diff, sensitive-data, and real-WPF UI theme/status gates pass. The host navigation item now uses a visible `SymbolIcon`/`Server24` without overriding `NavigationView` foreground states; the connection hint is tooltip-only. Explicit credentials have Valid/Invalid/Inconclusive validation, and WMI errors follow that evidence. All local mutation entrypoints use the process-wide host write lease; unexpected remote-console loss and post-configuration WMI loss reuse the stale-data, write-freeze, and automatic-reconnect state machine. Generated firewall commands reject ownership drift during compensation and rollback. The controlled-host runner validates evidence paths and supports optional A→B→A switching while remaining default-off.
- **Known issues**: Controlled host `10.0.0.6` is on the LAN; WMI/DCOM and TCP 2179 are available while TCP 445 remains closed. The latest read-only preview is `raw/controlled-host-readonly-current-20260815-1225.json`: the saved explicit credential reaches WMI/DCOM, both channels pass, interface `26` is Private on `10.0.0.0/24`, the atomic snapshot and real VM list each report one VM, and the plan contains exactly three firewall changes. Eight stages passed, five exact-confirmation scenarios were skipped, and zero failed. Issue #12 live apply/rollback and Issue #13 state-changing acceptance remain open because no exact-confirmation mutating scenario has run. The existing SSH.NET advisory remains out of scope.
- **Next action**: Keep the saved `Jwxa` credential for normal diagnostics; when a mutating acceptance is desired, run the requested scenario only after the product performs its read-only checks and the user enters exact Chinese `确认`.

## 2026-08-15 implementation continuation

- Re-ran the real-WPF fixture against the final build after the controlled-host recheck. Navigation reports a visible `SymbolIcon`/`Server24` at 16x17 inside a stable 40x40 item; inspected 720x900 light and 1100x800 dark overview renders show the theme-aware title, caution dot, U+EC61 green success, U+EB90 red failure, and tooltip-only disabled connection action without overlap.
- Ran a fresh production-adapter read-only acceptance after the final local build at `raw/controlled-host-readonly-continuation-20260815.json`. The saved explicit credential passed WMI/DCOM and TCP 2179 diagnostics, atomic activation/snapshot, one-VM query, console capture, and cleanup back to local: 7 stages passed, 6 exact-confirmation scenarios were skipped, 0 failed, and all four dangerous switches remained false.
- Revalidated the current tree after the UI consistency changes: the deterministic suite passes 178/178 and the Release WPF build completes with 0 errors and 248 existing warnings. The build used the repository's portable .NET 8 SDK plus the already generated COM interop wrappers because the system `dotnet` installation has no SDK and .NET Core MSBuild cannot resolve the project's COM references directly.
- Reconciled the task truth files with the live GitHub tracker. Issues #12 and #13 correctly remain OPEN because controlled apply/rollback, VM write, TCP 2179 degradation, disconnect recovery, and the required second-host exercise are not yet recorded. GitHub issues #2-#4 still show OPEN despite their locally validated `DONE` rows; no tracker state was changed while the implementation remains uncommitted.
- Replaced the remote-host navigation glyph with the shared WPF-UI `Server24` symbol and removed the item-level foreground override so selected, hover, light, and dark states come from `NavigationView`; aligned the page title with the existing `ui:TextBlock` heading style.
- Added real-main-window navigation smoke evidence in expanded/collapsed, light/dark, 1100x800/720x900 layouts. The remote-host icon is a visible `SymbolIcon`/`Server24` at 16x17 inside a stable 40x40 item.
- Rendered the remote-host overview with a deterministic WMI-success/TCP-2179-failure sample in all four theme/size combinations. The shared U+EC61 green success and U+EB90 red failure glyphs render correctly with no overlap; untested states retain the caution dot and the connection hint remains tooltip-only.
- Re-ran the latest production-adapter read-only acceptance against `10.0.0.6` at `raw/controlled-host-readonly-current-20260815-1225.json`: 8 stages passed, 5 mutating/outage/second-host opt-ins were skipped, and 0 failed. WMI/DCOM, TCP 2179, one-VM snapshot/list, console capture, interface 26, and the exact three-change configuration preview all passed with every danger switch false.
- Aligned the remote-host page with the existing WPF-UI visual language: the page title uses the theme primary foreground, navigation color states remain owned by `NavigationView`, connection eligibility is tooltip-only, and status rendering reuses the shared success/failure glyphs and caution/critical dots.
- Added regression assertions for theme-aware title/navigation foregrounds and the tooltip-only action row. Deterministic validation now passes `178/178`; the isolated Release build completes with 0 errors.
- Synchronized `design/remote-host-management` with the production UI contract: removed every legacy `E968` host glyph, reused the visible `E839` glyph, represented TCP 2179 failures with the existing red-cross status, and kept connection eligibility in a hover title. Static HTML/CSS, UTF-8 no-BOM, and diff checks pass; automated `file://` rendering was blocked by the browser URL policy and was not bypassed.
- Audited `raw/controlled-host-configuration-preview-20260815-113353.json`: the locale-independent snapshot count is `1`, the real remote VM list count is `1`, the preflight log identifies interface index `26`, the configuration plan remains exactly three changes, all mutation switches are false, and the UTF-8 no-BOM report contains no sensitive assignment.
- Added explicit network interface indexes to the shared Chinese preflight evidence, so a display name such as `jin-5g 3` cannot be confused with the actual interface index `26`.
- Revalidated the deterministic suite at `177/177` and the isolated Release product build at 0 errors.
- A read-only configuration preview with the stale index `3` was rejected before mutation with `选择的目标网络已不在最新检测结果中，请重新检测。`.
- A second read-only preview using the detected index `26` passed and produced exactly three planned changes: enable the built-in WMI rules, enable the built-in Hyper-V rules, and restrict the ExHyperV TCP 2179 rule to `10.0.0.0/24`. No remote command or rollback script was created.
- Preview evidence: `raw/controlled-host-configuration-preview-20260815-105847.json`.

## Session 2026-08-13

- Created the epic truth artifacts from approved GitHub issues #2-#13.
- Confirmed the pre-existing untracked `design/` and `doc/remote-host-management-spec.md` belong to earlier user work and must be preserved.
- Confirmed no existing test project and no persistent logger.
- Implemented and tested Issue #2; left its final validation active because the machine lacks the existing COM build tool `AxImp.exe`.
- Switched active work to independent Issue #3 so the roadmap can continue without hiding the build environment gap.
- Completed Issue #3 with 15/15 combined tests and a real isolated Windows Credential Manager round trip.
- Completed Issue #4 with 19/19 combined tests; the application now initializes a process-wide local session source of truth.
- Closed Issue #2 after a full 0-error WPF C# and XAML build using SHA-256-verified official COM tooling extracted without system installation.
- Completed Issue #5 with independent WMI/DCOM and TCP 2179 diagnostics, layout B connection UI, profile CRUD, 28/28 tests, full WPF compilation, and wide/narrow pixel verification.
- Completed Issue #6 with atomic host publication, write freeze/leases, per-session WMI identity contexts, basic snapshots, confirmed UI switching, and explicit local return; 38/38 tests and full validation pass.
- Completed Issue #7 with active-context VM lists and four remote lifecycle actions, generation-bound reads/writes, host-aware confirmations, stale UI clearing, and local-only feature gating; 46/46 tests and isolated WPF build pass. Controlled host `10.0.0.6` remains externally blocked on TCP 135/2179 and WMI RPC.
- Completed Issue #8 with immutable host-bound console captures, remote IPv4 TCP 2179 targets, generation-scoped windows, captured-context polling/lifecycle operations, and local-only action gating; 54/54 tests and full WPF build pass. Controlled-host proof remains blocked by a 3-second TCP 2179 timeout, and GitHub #7/#8 closure is pending API recovery.
- Completed Issue #9 with stale snapshot retention, write/console gates, single-owner capped-backoff reconnect, stop/retry controls, refreshed generation recovery, and responsive stale-state UI; 65/65 tests, WPF build, UTF-8/XAML/diff checks, and wide/narrow pixel inspection pass. Controlled-host TCP 135/2179 remain externally blocked.

## Session 2026-08-14 implementation audit

- Audited Issues #2-#13 against current implementation and tests without weakening the controlled-host gate.
- Centralized WMI query and method invocation options so every remote `WmiContext.OperationTimeout` reaches `ManagementObjectSearcher` and `InvokeMethodOptions`; local WMI behavior remains unchanged.
- Preserved WMI error source and status codes through remote VM reads and writes, allowing RPC transport failures to mark data stale, disable writes, and start the existing reconnect loop while ordinary business failures remain non-connection failures.
- Added ten focused regression tests, including Release RPC exception-chain and remote object-read timeout checks; the suite now passes 109/109. Release WPF build passes with 0 errors and 248 existing warnings; 32 XAML files parse; 11 touched files are UTF-8 without BOM; `git diff --check` passes.
- Added Issue #13 cross-module acceptance coverage and raised the deterministic suite to 115/115.
- Published the remote-host operations guide and synchronized the English README, Chinese README, and privacy policy with implemented behavior.
- Tightened TCP 2179 scope validation to reject CIDRs that cross RFC1918 boundaries, and centralized remote error redaction to prevent explicit credentials or secret-like fields from reaching UI, configuration, rollback scripts, or logs.
- Revalidated the final tree: Release build 0 errors, 32/32 XAML parse, 154 delivery text files UTF-8 without BOM, clean `git diff --check`, document sensitive-assignment scan 0 findings, and prior wide/narrow real-WPF screenshots remain valid.
- Left Issues #12 and #13 open because `10.0.0.6` is still unreachable at layer 2; no configuration command or rollback script was executed against the host.
- Completed a real-entrypoint audit and fixed diagnostic result ownership across profile-selection changes; revalidated 116/116 tests, Release build 0 errors, 32/32 XAML, UTF-8 without BOM, and clean diff checks.
- Rejected a prepared host-session candidate when the selected profile keeps the same ID but its address, display name, authentication mode, or credential reference changes before commit.
- Completed the WMI disconnect/cancellation audit: association queries now receive the remote enumeration timeout; cancelled class-method calls release late owned results; invalidated identity contexts cannot recreate cached `ManagementScope` entries.
- Preserved post-submission configuration safety by keeping remote process creation and marker/rollback tracking non-cancellable after the mutation boundary.
- Revalidated 118/118 tests, Release build 0 errors/248 existing warnings, 32/32 XAML, 152 delivery files UTF-8 without BOM, and clean `git diff --check`.
- Skipped launching a second smoke-test process because user-owned PID 42500 is still responsive and the current terminal is not elevated; no user process was terminated.
- Reconfirmed the controlled-host blocker: `10.0.0.6` neighbor state is `Incomplete`; ICMP, TCP 135, and TCP 2179 are unreachable. No credential, preflight, configuration, or rollback operation was attempted.
- Added the latest OPEN Issue #13 audit comment: https://github.com/jwxa/ExHyperV/issues/13#issuecomment-5285873205
- Added a repeatable, default-off controlled-host acceptance runner under `tests/ExHyperV.IntegrationTests`. It uses the production Windows adapters, writes a redacted Chinese JSON report, and places every mutating or outage scenario behind its own exact `确认` switch; the real-host gate remains OPEN.
- First read-only production-adapter run against `10.0.0.6` is captured at `raw/controlled-host-acceptance-20260814-051148.json`: ICMP is unreachable, WMI/DCOM and TCP 2179 time out, and preflight cannot open its read session. No mutation or outage switch was enabled.
- Added ten deterministic tests for the controlled-host runner and raised the suite to 128/128. The integration boundary now rejects non-exact confirmation, incompatible identity/credential variables, unsafe configuration indexes/CIDRs, and rollback without same-run configuration before network access; report metadata and detail keys are redacted and atomic cancellation preserves prior files.
- Revalidated default integration execution as `SKIP`, Release product build with 0 errors, touched test files as UTF-8 without BOM, and clean `git diff --check`. A 05:22 read-only recheck still shows incomplete neighbor resolution, 100% ICMP loss, and no TCP 135/2179 connection to `10.0.0.6`; no mutating action ran.
- Audited the remaining VM advanced-setting command entrypoints and added direct capability gates for CPU, memory, network, boot order, security, checkpoints, PCIe, GPU, storage, and VM creation. Added a source-level regression gate so representative local-only mutations cannot silently bypass the active-host capability matrix.
- Bounded application shutdown when a connector ignores cancellation and added a regression test proving shutdown returns without waiting indefinitely. The deterministic suite now passes 134/134.
- Revalidated the current tree: Release product build succeeds with 0 errors; 32/32 XAML files parse; the 15 latest touched implementation/test files are UTF-8 without BOM; `git diff --check` passes with line-ending notices only; the default controlled-host runner returns `SKIP` and performs no network access.
- Rechecked `10.0.0.6` read-only at 2026-08-14 06:05 +08:00 using the correct `Test-Connection -ComputerName` parameter. ICMP is false, the `WLAN 3` neighbor state is `Unreachable`, and TCP 135/2179 each time out after 3 seconds. No WMI, credential, configuration, VM mutation, outage, or rollback operation ran.
- Completion audit closed three local gaps: support artifacts can be opened/located with explicit results; malformed and unsupported `Hosts.xml` data has a stable Chinese error contract and remains untouched; every visible VM console button exposes the unified TCP 2179 capability reason when disabled.
- Final local baseline is 141/141 tests, Release build 0 errors, 32/32 XAML, 11/11 latest touched files UTF-8 without BOM, clean diff check apart from line-ending notices, default integration `SKIP`, and refreshed 1100x800/720x900 real-WPF configuration-result screenshots without overlap.
- Validation note: run the integration project and product WPF build serially because both transitively write `src/obj`; concurrent validation can temporarily corrupt WPF generated-file observations without changing source.
- Revalidated the resumed local baseline: 141/141 tests pass, the Release product build has 0 errors, and the default controlled-host runner returns `SKIP` without network access. Task CSV and UTF-8 truth-file checks pass.
- Rechecked `10.0.0.6` read-only at 2026-08-14 06:50 +08:00: ICMP had 100% loss, the on-link `WLAN 3` neighbor remained `Incomplete` with no MAC address, and TCP 135/2179 timed out. No WMI, credential, configuration, VM mutation, outage, or rollback action ran.
- Revalidated after enabling TCP 2179 revalidation on first confirmed host activation: 144/144 deterministic tests pass, the Release product build has 0 errors, and the default controlled-host runner returns `SKIP` without network access. `git diff --check` passes with only existing line-ending notices.
- The live controlled-host gate remains open: `10.0.0.6` was not probed again with mutation enabled; no credential, configuration, VM mutation, outage, or rollback action ran.
- Rechecked `10.0.0.6` read-only at 2026-08-14 07:13 +08:00: ICMP remained false, the `WLAN 3` neighbor was `Unreachable` with an all-zero link-layer address, and TCP 135/2179 each timed out after 3 seconds. The workflow stopped before WMI and credentials; no mutating action ran.
- Added controlled-host evidence gates for two-level capped reconnect backoff and TCP 2179 partial availability. Hardened configuration safety by atomically prewriting the current command's idempotent rollback before remote submission and conservatively retaining uncertain entries. The deterministic suite now passes 149/149; Issue #12/#13 remain open pending real-host evidence.
- Rechecked `10.0.0.6` read-only at 2026-08-14 07:44 +08:00: ICMP remained false, the `WLAN 3` neighbor was `Unreachable` with an all-zero link-layer address, and TCP 135/2179 timed out after 3016/3000 ms. No WMI, credential, configuration, VM mutation, outage, or rollback action ran.
- Corrected the native `MSFT_NetFirewallRule.Enabled` mapping from direct Windows runtime evidence: `1=True`, `2=False`; verified both System.Management and NetSecurity report the same 420 enabled and 163 disabled inbound rules on this host.
- Normalized native firewall protocol numbers to NetSecurity names, rejected policy-managed disabled built-in rules, and detected fixed-name TCP 2179 rules across both directions before allowing mutation.
- Added in-memory PowerShell execution tests that prove the generated TCP 2179 script applies and precisely restores Enabled/Action/Profile/Protocol/LocalPort/RemoteAddress, compensates an address-filter failure, and disables only rules enabled by a partially failed built-in-rule operation.
- Current local baseline: 155/155 deterministic tests; Release build 0 errors/248 existing warnings; 32/32 XAML; 4/4 latest touched files UTF-8 without BOM; 13/13 task CSV files parse; clean `git diff --check`; default integration runner returns `SKIP`. Issue #12/#13 remain open because no newer controlled-host reachability or live-mutation evidence exists.
- Rechecked `10.0.0.6` read-only after the 155-test baseline at 2026-08-14 08:35 +08:00: ICMP remained false, the `WLAN 3` neighbor was `Unreachable` with an all-zero link-layer address, and TCP 135/2179 timed out after 3016/3000 ms. The workflow stopped before WMI and credentials; no configuration, VM mutation, outage, or rollback action ran.
- Fixed generated built-in firewall apply assertions to evaluate the active `Where-Object` pipeline rule rather than an unrelated `$rule` variable, and reused the same ownership predicate for partial-failure compensation.
- Added runtime PowerShell coverage proving that replaced TCP 2179 rules are not deleted, changed built-in rule ownership is not disabled, Token policy values `0`/`2` are not removed, and `DomainAuthenticated` networks are not reset to Public.
- Current local baseline: 160/160 deterministic tests; Release build 0 errors/248 existing warnings; 32/32 XAML; 6/6 latest touched source/test/docs files UTF-8 without BOM; 13/13 task CSV files parse; clean `git diff --check`; default integration runner returns `SKIP`.
- Rechecked `10.0.0.6` read-only at 2026-08-14 09:55 +08:00: ICMP remained false, the `WLAN 3` neighbor was `Unreachable` with an all-zero link-layer address, and TCP 135/2179 timed out after 3015/3000 ms. The workflow stopped before WMI and credentials; no configuration, VM mutation, outage, or rollback action ran.
- Closed the Issue #13 local audit gap: the controlled-host runner now persists and reloads the product host profiles and, when `EXHYPERV_INTEGRATION_SECOND_HOST` is supplied, performs and reports a real A→B→A active-host switch plus a second-host VM read. Missing second-host input is explicitly reported as skipped/partial.
- Added deterministic second-host option validation for missing pairs, duplicate normalized addresses, duplicate profile IDs, and Credential Manager requirements. The local suite is now 160/160; Release build 0 errors; default integration `SKIP`.
- Final read-only recheck at 2026-08-14 10:28 +08:00: ICMP false, `WLAN 3` neighbor `Unreachable` with `00-00-00-00-00-00`, TCP 135 false after 3013 ms, and TCP 2179 false after 3000 ms. No WMI, credentials, configuration, VM mutation, outage, or rollback action ran.
- Continued read-only recheck at 2026-08-14 10:33 +08:00: ICMP false, `WLAN 3` neighbor `Incomplete` with `00-00-00-00-00-00`, TCP 135 false after 3037 ms, and TCP 2179 false after 3033 ms. The workflow stopped before WMI and credentials; no mutation ran.
- Continued implementation validation: `IntegrationOptions` now clears the one-time password environment variable immediately after capture, including validation-failure paths. Deterministic tests pass 161/161, the default integration runner returns `SKIP`, and the Release product build has 0 errors.
- Hardened controlled-host evidence startup: report paths are normalized and must name a `.json` file; profile evidence persistence failure now skips every network-dependent stage before any probe or mutation. Invalid-path execution exits with code 2 before the runner starts. Deterministic tests pass 162/162, default integration remains `SKIP`, and Release build has 0 errors.
- Fixed the production post-configuration disconnect path: an active host whose WMI/DCOM recheck fails now retains its last snapshot as stale, freezes writes, and starts the existing single-owner capped-backoff reconnect loop. Deterministic tests pass 163/163, default integration returns `SKIP`, and Release build has 0 errors.
- Hardened structured logging so both property names and property values pass through the centralized sensitive-data redactor before serialization. A regression test covers a secret embedded in a property key; deterministic validation remains 163/163 and the Release build passes with 0 errors (the existing SSH.NET `NU1903` warning remains out of scope).
- Rechecked `10.0.0.6` read-only at 2026-08-14 11:05 +08:00: ICMP remained false, the `WLAN 3` neighbor was `Unreachable` with `00-00-00-00-00-00`, and TCP 135/2179 timed out after 3012/3000 ms. The workflow stopped before WMI and credentials; no configuration, VM mutation, outage, or rollback action ran.
- Completed the local standards/spec audit: sensitive values containing spaces are fully redacted; late VM-list mapping rechecks the active generation; local host/hardware and every representative VM advanced mutation acquire and dispose a global host write lease; the host selector is disabled during switching; and unexpected remote-console loss enters the existing reconnect coordinator only when the VM is still running or management is also unavailable.
- Kept per-command partial-failure compensation in the configuration runner. It restores only the command whose commit state could not be recorded and remains covered by ownership-drift tests; it is distinct from executing the generated full rollback script, which still requires the user's exact Chinese `确认`.
- Current local baseline is 167/167 deterministic tests, Release build 0 errors/248 existing warnings, and default controlled-host integration `SKIP` with no network access. Live gates remain open and no newer host probe or mutation was performed.

## Session 2026-08-15 controlled-host recovery

- Confirmed the controlled host is back on the LAN: TCP 135 and TCP 2179 both accept connections from `10.0.0.14` through `WLAN 3`. ICMP returned a local PowerShell resource error, so channel-specific TCP evidence is authoritative.
- Ran the production-adapter controlled-host entry point with current Windows identity and every danger switch false. `raw/controlled-host-acceptance-20260815-002906.json` records TCP 2179 success and WMI/DCOM `AuthenticationFailed`; no configuration, VM write, outage, or rollback action ran.
- Reproduced the management failure independently: read-only queries to both `root\cimv2` and `root\virtualization\v2` return `0x80070005` (`Access Denied`) for `JWXA-MINIPC\jwxa`. The blocker has moved from network reachability to target identity/authorization.
- Loaded the product profile `Jwxa-HyperVM` and its remembered Credential Manager entry without logging its secret. `raw/controlled-host-acceptance-20260815-010240.json` records the same WMI/DCOM `AuthenticationFailed` result for explicit account `Jwxa`, while TCP 2179 remains available and all danger switches remain false.
- Resolved the target PTR as `Jwxa-Laptop.lan`; a single in-memory, read-only WMI test with target-qualified `Jwxa-Laptop\Jwxa` also returned Access Denied. No credential was rewritten and no target state changed.
