# Progress Log

## Context Recovery Block

- **Current milestone**: #4 - Run controlled-host end-to-end acceptance
- **Current status**: IN_PROGRESS
- **Last completed**: Issue #13 local release/UI completion audit and support-artifact workflow
- **Current artifact**: `.codex-tasks/remote-host-management/tasks/issue-13-release/TODO.csv`
- **Key context**: Deterministic validation passes 178/178. Release build and local static gates pass, including shared theme/status visuals, tooltip-only connection eligibility, native Windows firewall semantics, generated-script rollback ownership guards, evidence-path fail-fast behavior, optional two-host switching, one-time-password lifetime clearing, global host-write leases for local mutations, and automatic reconnect after post-configuration WMI or unexpected remote-console loss. Explicit credentials have Valid/Invalid/Inconclusive validation; the controlled runner keeps SMB diagnostic use separate from the WMI/DCOM + TCP 2179 management design. Controlled disconnect acceptance still requires old-data/write-freeze/no-local-fallback evidence, two increasing delays capped at 30 seconds, a fresh generation, a refreshed snapshot, and restored capabilities. TCP 2179 available and unavailable must be recorded separately.
- **Known blocker**: `10.0.0.6` is on the LAN and WMI/DCOM plus TCP 2179 are available, but TCP 445 is closed. The latest report is `../../raw/controlled-host-configuration-preview-20260815-113353.json`: explicit credential pre-validation is Inconclusive, then preflight, activation, one-VM snapshot, real one-VM read, console capture, and a three-change configuration preview succeed with no mutation switch enabled. Controlled-host acceptance remains open pending explicitly confirmed state-changing scenarios.
- **Next action**: Keep the runner read-only by default; when a scenario is requested, rerun the checks, display the full Chinese log, and require exact Chinese `确认` before VM writes, configuration, outage, or rollback verification.

## 2026-08-15 implementation continuation

- The latest read-only preview passed on the detected interface index `26` and generated the same three-change plan without enabling any danger switch.
- A deliberately stale index `3` was rejected before remote configuration, proving the runner binds the plan to the current preflight selection.
- Evidence: `../../raw/controlled-host-configuration-preview-20260815-105847.json`.

## Session 2026-08-14

- Added cross-module end-to-end coverage; the deterministic suite passes 116/116.
- Fixed RFC1918 CIDR validation so supernets such as `10.0.0.0/7` cannot open TCP 2179 outside private address space.
- Applied centralized sensitive-data redaction across diagnostics, configuration, switching, reconnect, VM operations, and UI fallback errors.
- Published English and Chinese README updates, corrected the privacy policy, and added the remote-host operations guide.
- Release WPF build passes with 0 errors and 248 existing warnings. All 32 XAML files parse, 154 delivery text files are UTF-8 without BOM, `git diff --check` passes, and five documents contain no secret-like assignments.
- Controlled-host acceptance remains open because `10.0.0.6` is unreachable at layer 2; no remote configuration or rollback was attempted.
- Audited the real WPF navigation, active-session, VM, console, configuration, rollback, log, and credential paths against the specification.
- Fixed a UI diagnostic race: selecting host B now invalidates host A's in-flight diagnostic ownership, so a late A result cannot overwrite B's detail panel or connection evidence. The replacement run remains independently current.
- Revalidated after the race fix: 116/116 tests, Release build 0 errors and 248 existing warnings, 32/32 XAML parse, touched files UTF-8 without BOM, and `git diff --check` pass.
- Rejected stale host-switch candidates when a saved profile keeps the same ID but changes address, display name, or identity configuration.
- Made remote WMI association queries inherit `OperationTimeout`, propagated cancellation through owned class-method results, and added delayed disposal when cancellation wins the race.
- Made `WmiContext` invalidation permanent so a late background WMI task cannot repopulate the connection cache after its preflight, configuration, or host-session owner has been disposed.
- Preserved the configuration safety boundary: after `Win32_Process.Create` is submitted, the runner remains non-cancellable until it records the remote marker and rollback state.
- Final local result: 118/118 tests; Release build 0 errors and 248 existing warnings; 32/32 XAML parse; 152 delivery files UTF-8 without BOM; `git diff --check` passes.
- Did not launch a second smoke-test instance because the user-owned ExHyperV process PID 42500 remains responsive; the current terminal is not elevated and the application requires administrator elevation.
- Rechecked controlled host `10.0.0.6`: neighbor state remains `Incomplete`; ICMP, TCP 135, and TCP 2179 are all unreachable, so no credential, preflight, configuration, or rollback action ran.
- Published the updated audit evidence without closing Issue #13: https://github.com/jwxa/ExHyperV/issues/13#issuecomment-5285873205
- Added `tests/ExHyperV.IntegrationTests`, an explicit opt-in Windows/Hyper-V controlled-host runner that directly reuses production IPv4, WMI/DCOM, TCP 2179, session activation, snapshot, VM query, console capture, configuration, and reconnect adapters.
- The runner performs no network access unless `EXHYPERV_INTEGRATION_RUN=确认`. VM writes, operator-controlled outage, remote configuration, and rollback verification each require an independent exact `确认` environment switch.
- Current Windows identity is the default. Explicit credentials can be resolved from the existing Credential Manager target, or passed once through a process environment variable that is cleared after capture and never serialized into the structured report.
- The runner emits an atomic UTF-8 JSON report with Chinese stage results and redacted diagnostic/preflight detail. Default execution returns `SKIP`; Release compilation has 0 errors. Live execution is still pending because `10.0.0.6` remains unreachable.
- Ran the new production-adapter entry point read-only against `10.0.0.6` using the current Windows identity. `.codex-tasks/remote-host-management/raw/controlled-host-acceptance-20260814-051148.json` records ICMP `DestinationHostUnreachable`, WMI/DCOM timeout, TCP 2179 timeout, and read-only preflight connection failure. The report is UTF-8 without BOM, contains no secret-like assignments, and shows every danger switch as false.
- Linked the integration runner's option and report models into the deterministic test executable and added ten focused safety tests. The suite now passes 128/128 and covers exact `确认` gates, current-identity/credential-manager boundaries, rollback/configuration coupling, interface/CIDR validation, password-safe formatting, status calculation, atomic UTF-8 output, cancellation preservation, and top-level/detail redaction.
- Tightened integration-only configuration parsing so interface index zero, empty index lists, malformed/public/duplicate CIDRs, and Public-to-Private selections outside the configured interface set fail before any network access. Report metadata and sensitive detail keys now use the same central redactor as production logs.
- Revalidated the integration project default gate (`SKIP` with no network access), Release product build (0 errors; only the existing SSH.NET NU1903 warning), UTF-8 without BOM for all touched test files, and `git diff --check`.
- Rechecked `10.0.0.6` at 2026-08-14 05:22 +08:00: neighbor resolution remains incomplete, ICMP has 100% packet loss, and TCP 135/2179 do not connect. No live WMI, VM write, configuration, outage, or rollback action was attempted.
- Added direct capability checks to representative local-only VM mutation entrypoints and a source-level regression test that prevents those gates from being removed accidentally.
- Made active-session shutdown bounded when a connector ignores cancellation and added focused regression coverage. The deterministic suite now passes 134/134.
- Revalidated Release build with 0 errors, 32/32 XAML parsing, 15/15 latest touched files as UTF-8 without BOM, and clean `git diff --check` apart from line-ending notices. Default integration execution returns `SKIP` and does not access the network.
- Rechecked `10.0.0.6` read-only at 2026-08-14 06:05 +08:00 with the correct ICMP parameter: ICMP false, `WLAN 3` neighbor `Unreachable`, TCP 135 timeout, and TCP 2179 timeout. No live WMI, credential, VM write, configuration, outage, or rollback action ran.
- Completion audit reopened step #3: the existing log-directory button calls a silent shell helper without behavior coverage, while the generated rollback script is displayed only as text and cannot be directly located from the remote-host module. Implement a narrow support-artifact locator, wire both commands, and validate success/missing/failure behavior before restoring the local release gate.
- Added `ISupportArtifactLocator` with a Windows Explorer adapter. The host connection page now opens the real log directory with explicit success/failure feedback and can locate a generated rollback script from an icon button beside its path.
- Added four behavior tests for Explorer arguments, file selection, missing paths, launch failures, and error redaction. Updated the operations guide with both UI entrypoints.
- Fixed the ordinary VM-card console button so it remains visible but exposes the same `IsConsoleAvailable` gate and `ConsoleUnavailableText` tooltip as the remote template. A source-level XAML contract protects both visible buttons.
- Normalized malformed `Hosts.xml` errors and proved malformed/unsupported data never rewrites either `Hosts.xml` or adjacent `Config.xml`.
- Revalidated 141/141 tests, Release product build with 0 errors, 32/32 XAML, 11/11 touched files as UTF-8 without BOM, and `git diff --check`. Refreshed real-WPF success-result screenshots at 1100x800 and 720x900 show the rollback path, locator button, instructions, log panel, and danger action without overlap.
- Do not run the integration project and product WPF build concurrently because both transitively write `src/obj`; the observed temporary `App.OnStartup`/`OnExit` override error disappeared under the required serial validation and is not a source defect.
- Resumed verification passes 141/141 deterministic tests; the Release product build has 0 errors; the default controlled-host runner returns `SKIP` without network access.
- The 2026-08-14 06:50 +08:00 read-only probe still fails below WMI/DCOM: ICMP had 100% loss, the on-link neighbor remained `Incomplete`, and TCP 135/2179 timed out. No credential or danger switch was used.
- First confirmed host activation and the controlled-host acceptance activation now request a fresh TCP 2179 probe; this closes the stale-console-evidence race between diagnostics and switch commit.
- Local verification after the change is 144/144 deterministic tests, Release build 0 errors, and default integration `SKIP`; controlled-host acceptance remains pending until `10.0.0.6` is reachable.
- A 2026-08-14 07:13 +08:00 read-only recheck still reports ICMP false, `WLAN 3` neighbor `Unreachable`, and TCP 135/2179 timeouts. The runner was not enabled and no credential or mutating action was attempted.
- Added deterministic acceptance for the controlled disconnect observer and the WMI-available/TCP-2179-unavailable degradation path. The runner now asks the operator to restore networking only after observing at least two increasing retry delays and reports all recovery evidence explicitly. Local validation is 149/149; the live controlled-host gate remains open.
- Revalidated the local release after correcting Windows firewall native-state handling: 155/155 deterministic tests, Release build 0 errors/248 existing warnings, 32/32 XAML, 13/13 task CSV files, UTF-8, and `git diff --check` pass. The default integration runner returns `SKIP`; no network or mutation opt-in was supplied.
- The 2026-08-14 08:35 +08:00 read-only recheck remained blocked below WMI/DCOM: ICMP false, `WLAN 3` neighbor `Unreachable` with an all-zero MAC, and TCP 135/2179 timeouts after 3016/3000 ms. No credentials or controlled-host mutation path was entered.
- The 2026-08-14 07:44 +08:00 read-only recheck still fails before WMI/DCOM and credentials: ICMP false, an `Unreachable` all-zero neighbor, and TCP 135/2179 timeouts. No mutating or outage switch was enabled.
- Hardened generated configuration scripts against apply, compensation, and rollback ownership drift. Runtime tests now also prove that replaced TCP 2179 rules, changed Token policy values, and `DomainAuthenticated` networks are preserved.
- Revalidated 159/159 deterministic tests, Release build with 0 errors/248 existing warnings, 32/32 XAML, 13/13 task CSV files, UTF-8, and `git diff --check`. The default controlled-host runner returns `SKIP`.
- The 2026-08-14 09:55 +08:00 read-only recheck remained blocked below WMI/DCOM: ICMP false, `WLAN 3` neighbor `Unreachable` with an all-zero MAC, and TCP 135/2179 timeouts after 3015/3000 ms. No credentials or mutating path was entered.
- Implemented the missing two-host controlled acceptance path: profiles are saved/reloaded through `HostProfileStore`, and an optional second host is diagnosed, activated, read through, then switched back to the first host with generation and snapshot evidence. Added docs and deterministic option-boundary coverage; local suite is 160/160.
- Final read-only recheck at 2026-08-14 10:28 +08:00 still fails below WMI/DCOM: ICMP false, `WLAN 3` neighbor `Unreachable` with an all-zero MAC, and TCP 135/2179 timeouts after 3013/3000 ms. No live credentials, configuration, VM mutation, outage, or rollback path was entered.
- Continued read-only recheck at 2026-08-14 10:33 +08:00 still fails below WMI/DCOM: ICMP false, `WLAN 3` neighbor `Incomplete` with an all-zero MAC, and TCP 135/2179 timeouts after 3037/3033 ms. No live credentials, configuration, VM mutation, outage, or rollback path was entered.
- Continued local validation passes 161/161 deterministic tests, the default integration gate returns `SKIP`, and the Release product build has 0 errors. The integration option loader now clears a captured one-time password before any subsequent validation error can expose it; controlled-host acceptance remains pending.
- Controlled-host evidence is now fail-fast: custom report paths must identify a `.json` file, are normalized before use, and a profile-evidence write failure skips all network-dependent stages. Invalid-path execution was verified to exit with code 2 before the runner starts. Deterministic tests pass 162/162 and Release build has 0 errors.
- Production configuration refresh now routes WMI/DCOM loss through the same stale-data, write-freeze, and automatic-reconnect state machine used by VM operation failures. A focused coordinator test proves the reconnect attempt starts and old snapshot remains intact. Deterministic tests pass 163/163 and Release build has 0 errors.
- Structured log serialization now redacts both property names and property values, closing the case where a secret-like assignment is accidentally supplied as a property key. The focused regression passes within the 163/163 deterministic suite, and the post-fix Release build has 0 errors with only the existing out-of-scope SSH.NET `NU1903` warning.
- The 2026-08-14 11:05 +08:00 read-only recheck remained blocked below WMI/DCOM: ICMP false, `WLAN 3` neighbor `Unreachable` with an all-zero link-layer address, and TCP 135/2179 timeouts after 3012/3000 ms. No credential or mutating acceptance switch was used.
- Completed the local release audit fixes: password-like values with spaces are fully redacted; late VM mapping is generation-safe; all local host/hardware and VM advanced mutations hold global write leases; host selection is disabled during an active switch; unexpected remote-console failures report into the reconnect coordinator after excluding stopped/deleted VMs; and a planned TCP 2179 outage no longer fails the separate available-channel capture stage.
- Revalidated 167/167 deterministic tests, Release build with 0 errors/248 existing warnings, and the default controlled-host runner as `SKIP` without network access. Live controlled-host evidence remains unchanged and open.

## 2026-08-15 controlled-host recovery checkpoint

- The host is no longer blocked at layer 2: TCP 135 and TCP 2179 both connect from `10.0.0.14` over `WLAN 3`.
- Ran the production entry point read-only with current Windows identity and all danger switches false. `../../raw/controlled-host-acceptance-20260815-002906.json` records TCP 2179 success, WMI/DCOM `AuthenticationFailed`, and safe skips for activation, VM reads/writes, configuration, outage, and rollback stages.
- Independent read-only WMI queries to `root\cimv2` and `root\virtualization\v2` both return `0x80070005`. Live acceptance remains open until an authorized target identity is supplied.
- The remembered explicit account `Jwxa` was resolved successfully, but `../../raw/controlled-host-acceptance-20260815-010240.json` records the same WMI/DCOM denial with TCP 2179 success and every danger switch false. A single target-qualified `Jwxa-Laptop\Jwxa` test was also denied without changing credentials or target state.
- Re-ran the production-adapter path after the UI consistency changes. `../../raw/controlled-host-acceptance-20260815-104032.json` records successful WMI/DCOM and TCP 2179 diagnostics, read-only preflight, atomic activation, one remote VM, TCP 2179 console capture, and explicit cleanup back to local. All four danger switches are false and the report contains no secret-like assignment.
